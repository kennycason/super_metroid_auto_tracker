<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    
    <!-- Disable JNDI lookup to avoid java.naming dependency -->
    <contextListener class="ch.qos.logback.classic.jul.LevelChangePropagator">
        <resetJUL>true</resetJUL>
    </contextListener>
    
    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} -- %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- File Appender with timestamp -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- Use ~/.smtracker/ directory for logs -->
        <file>${user.home}/.smtracker/smtracker.log</file>
        
        <!-- Rolling policy: keep 10 days of logs, max 10MB per file -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${user.home}/.smtracker/smtracker-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>10</maxHistory>
            <totalSizeCap>100MB</totalSizeCap>
        </rollingPolicy>
        
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} -- %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- Root logger configuration -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>
    
    <!-- More verbose logging for our app components -->
    <logger name="com.supermetroid" level="INFO" />
    
    <!-- Less verbose logging for external libraries -->
    <logger name="org.jetbrains.compose" level="WARN" />
    <logger name="io.grpc" level="WARN" />
    <logger name="io.netty" level="WARN" />
    
</configuration>


